# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.



cd /home/abir/Projects/react/BudgetWise/helm/budgetwise && helm install budgetwise . --set image.repository=localhost:5000/budgetwise   --set image.tag=1.0.0   --set image.pullPolicy=Always   --set seed.enabled=false